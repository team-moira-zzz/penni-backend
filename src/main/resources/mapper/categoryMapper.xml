<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moira.pennibackend.domain.category.mapper.CategoryMapper">
    <!-- ========================================================================================================== -->
    <!-- 카테고리 목록 조회 -->
    <select id="selectCategoryList" resultType="com.moira.pennibackend.domain.category.dto.response.CategoryResponse">
        SELECT
               id
             , type
             , name
             , display_order as displayOrder
        FROM   "ACCOUNT_BOOK_CATEGORY"
        WHERE  group_id = #{groupId} AND type = #{type}
    </select>

    <!-- ========================================================================================================== -->
    <!-- 마지막 정렬번호 조회 -->
    <select id="selectLastDisplayOrder" resultType="java.lang.Long">
        SELECT display_order
        FROM   "ACCOUNT_BOOK_CATEGORY"
        WHERE  group_id = #{groupId}
        ORDER BY display_order DESC
        LIMIT 1
    </select>

    <!-- ========================================================================================================== -->
    <!-- 카테고리 추가 -->
    <insert id="insertCategory" parameterType="com.moira.pennibackend.global.entity.AccountBookCategory">
        INSERT INTO "ACCOUNT_BOOK_CATEGORY" (
               type
             , group_id
             , name
             , display_order
             , created_at
        )
        VALUES (
               #{type}
             , #{groupId}
             , #{name}
             , #{displayOrder}
             , NOW()
        )
    </insert>

    <update id="updateCategoryOrder">
        UPDATE "ACCOUNT_BOOK_CATEGORY"
        SET    display_order = #{displayOrder}
        WHERE  group_id = #{groupId} AND id = #{categoryId} AND type = #{type}
    </update>

    <!-- ========================================================================================================== -->
    <!-- 카테고리 삭제 -->
    <delete id="deleteCategory">
        DELETE FROM "ACCOUNT_BOOK_CATEGORY" WHERE group_id = #{groupId} AND id = #{categoryId}
    </delete>
</mapper>